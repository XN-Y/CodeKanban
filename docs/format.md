对于给定的<文本区></文本区>，总结一个范式给我：

<文本区>
───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
←  ☐ 文件清理  ☐ Git 提交  ✔ Submit  →

我发现项目中有一些临时文件和目录（如 tmp/, nul, 1.txt 等）。是否需要清理这些文件？

❯ 1. 是，清理所有临时文件
     删除 tmp/, nul, 1.txt 等临时文件和测试文件
  2. 否，保留这些文件
     暂时不做任何清理操作
  3. 部分清理
     让我手动选择要删除哪些文件
  4. Type something.

Enter to select · Tab/Arrow keys to navigate · Esc to cancel
</文本区>



以下为标准示例，你应当参考格式：
<文本区>

> 等待30s

· Incubating… (esc to interrupt)
  ⎿  Tip: Use /stickers to order Claude Code swag

───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
>
───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  ⏵⏵ bypass permissions on (shift+tab to cycle)

</文本区>

<范式>
对于 claude code，在当前所有行内，从后前向前找，先找一行 '─' * 终端宽度
文本，接着向上继续搜，再找一行同样的文本，此时文本框就被定位了，这中间的文本是当前用户正在输入的文本。

如果能定位到这个，能确认到目前的状态是等待输入或者工作中。

然后继续逆着向上，如果定位到:
"  ⎿  Tip: "

就继续向上定位:
如果再有
            regexp.MustCompile(`^[✻✶∴·○◆▪▫□■☐☑☒★☆✓✔✗✘⚬⚫⚪⬤◯▸▹►▻◂◃◄◅✢*]\s+.+…\s*\(esc\s+to\s+interrupt`),
这个就是工作中。

如果没有 "  ⎿  Tip: " 出现，那么可以判断为等待输入。等待输入也是默认的状态
</范式>
